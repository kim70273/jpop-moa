# Gemini 프로젝트 가이드라인

이 문서는 현재 프로젝트의 아키텍처 및 코딩 컨벤션을 정의합니다. 저(AI 어시스턴트)는 앞으로의 모든 개발 과정에서 이 가이드라인을 엄격히 준수할 것입니다。

## 1. 프로젝트 구조: Feature-Sliced Design (FSD)

이 프로젝트는 [Feature-Sliced Design (FSD)](https://feature-sliced.design/) 방법론을 따릅니다. 코드는 모듈성, 확장성, 유지보수성을 높이기 위해 계층(Layer)과 슬라이스(Slice)로 구성됩니다.

주요 디렉터리 구조는 `src/` 내에 위치합니다.

-   `app`: 애플리케이션의 최상위 계층으로, 전역 스타일, 프로바이더, 루트 레이아웃 등을 포함합니다.
-   `widgets`: 여러 기능과 엔티티를 조합하여 만드는 UI의 독립적인 블록입니다. (예: 헤더, 사이드바, 게시글 목록)
-   `features`: 사용자의 특정 유스케이스에 해당하는 기능입니다. (예: 인증, 게시글 작성, 검색)
-   `entities`: 애플리케이션 전반에서 사용되는 비즈니스 핵심 데이터 모델입니다. (예: 사용자, 게시글, 댓글)
-   `shared`: 특정 비즈니스 로직에 종속되지 않는 재사용 가능한 코드입니다. (예: UI 컴포넌트, 유틸리티 함수, 설정)

**규칙:**

-   각 계층은 자신보다 엄격하게 아래에 있는 계층만 참조할 수 있습니다. (예: `widgets`는 `features`, `entities`, `shared`를 임포트할 수 있지만, `app`은 임포트할 수 없습니다.)
-   같은 계층 내의 슬라이스들은 서로를 직접 참조해서는 안 됩니다.

## 2. 데이터 페칭: TanStack Query (React Query)

모든 서버 데이터 요청 및 캐싱은 `@tanstack/react-query`를 사용하여 처리해야 합니다.

-   **커스텀 훅**: 각 쿼리 및 뮤테이션에 대한 커스텀 훅을 생성합니다. (예: `useGetUser`, `useUpdatePost`) 이 훅들은 쿼리 키, fetcher 함수 및 기타 설정을 캡슐화해야 합니다.
-   **쿼리 키**: 구조적이고 일관된 쿼리 키를 사용합니다. 엔티티 이름으로 시작하고 파라미터를 포함하는 배열 형태를 권장합니다. (예: `['users', userId]`, `['posts', { category: 'tech' }]`)
-   **위치**: 데이터 페칭 로직(커스텀 훅, 쿼리 키 등)은 관련된 `entity` 또는 `feature` 슬라이스 내에 위치해야 합니다.

## 3. 로컬 상태 관리: Zustand

클라이언트 측의 로컬 상태는 `zustand`를 사용하여 관리합니다.

-   **스토어**: 각 상태 도메인별로 개별 스토어를 생성합니다.
-   **위치**: Zustand 스토어는 관련된 `feature` 또는 `entity` 슬라이스 내에 위치해야 합니다. 만약 여러 기능에서 공통으로 사용된다면 `shared` 슬라이스에 위치시킬 수 있습니다.

## 4. 런타임 타입 검사: Zod

API 응답, 폼 제출 등 런타임에 데이터 유효성을 검사하기 위해 `zod`를 사용합니다.

-   **스키마**: 모든 외부 데이터 구조에 대한 Zod 스키마를 정의합니다.
-   **타입 추론**: Zod 스키마로부터 TypeScript 타입을 추론하여 코드의 일관성을 유지하고 보일러플레이트를 줄입니다. (`z.infer<typeof mySchema>`)
-   **위치**: Zod 스키마는 관련된 `entity` 또는 `feature` 내에 정의합니다.

## 5. 스타일링: Tailwind CSS

모든 스타일링은 [Tailwind CSS](https://tailwindcss.com/)를 사용합니다.

-   **Utility-First**: 유틸리티 우선 접근 방식을 따릅니다. 가능한 한 별도의 CSS 파일을 작성하는 것을 지양합니다.
-   **테마**: `tailwind.config.js` 파일을 수정하여 프로젝트 고유의 디자인 토큰(색상, 간격, 폰트 등)을 정의합니다.

## 6. 린팅 및 포맷팅

-   **ESLint**: `eslint-config-next` 프리셋을 기반으로 ESLint가 설정되어 있습니다. 모든 코드는 이 규칙을 준수해야 합니다.
-   **Prettier**: 코드 포맷팅은 Prettier가 담당합니다. 일관된 코드 스타일을 위해 커밋 전에 `npm run format`을 실행합니다.

## 7. API 사용

이 프로젝트는 대한민국 내에서 서비스될 예정입니다.

-   **한국어 응답 우선**: Open API를 사용할 경우, 가능한 한 응답이 한국어로 제공되는 API를 우선적으로 사용합니다.
-   **번역 필요 명시**: 만약 한국어를 지원하지 않는 API를 사용해야 할 경우, 해당 데이터를 사용자에게 보여주기 전에 번역 과정이 필요하다는 점을 코드나 주석에 명확히 명시해야 합니다。

---

## 백엔드 개발 계획

아래 계획에 따라 백엔드 개발을 진행합니다.

-   **기술 스택**: NestJS, PostgreSQL

### 1단계: 백엔드 및 데이터베이스 환경 설정 (완료)
-   **내용**: NestJS 프로젝트를 생성하고 PostgreSQL 데이터베이스와의 연동을 설정합니다.
-   **세부 작업**:
    -   NestJS CLI를 사용하여 신규 프로젝트 생성
    -   TypeORM을 사용하여 PostgreSQL 연결 설정
    -   `Artist` 및 `News` 엔티티 정의 및 관계 설정
    -   `.env` 파일을 통한 데이터베이스 환경 변수 관리

### 2단계: 웹 크롤러 개발 (완료)
-   **내용**: J-pop 소식 원문을 수집하기 위한 웹 크롤러를 개발합니다.
-   **세부 작업**:
    -   `axios` 및 `cheerio`를 사용하여 웹페이지 HTML 콘텐츠 페칭 및 파싱 기능 구현
    -   `CrawlerModule`, `CrawlerService`, `CrawlerController` 설정

### 3단계: AI를 이용한 정보 가공 및 번역 (완료)
-   **내용**: 수집된 원문 데이터를 AI를 통해 가공하고 번역합니다.
-   **세부 작업**:
    -   `@google/generative-ai` SDK 설치 및 `GEMINI_API_KEY` 설정
    -   `AiModule`, `AiService`, `AiController` 설정
    -   Gemini API를 호출하여 HTML 콘텐츠에서 뉴스 정보 추출 및 한국어로 번역 기능 구현
    -   AI 모델의 JSON 출력 형식 강화를 위한 프롬프트 개선 및 안전 설정 적용

### 4단계: API 서버 개발 및 프론트엔드 연동 (완료)
-   **내용**: 프론트엔드에서 사용할 API를 개발하고 데이터를 제공합니다.
-   **세부 작업**:
    -   `NewsService`에 `saveProcessedNews` 메서드 구현 (크롤링, AI 처리 후 데이터베이스 저장)
    -   `NewsController`에 `POST /news/process` 엔드포인트 구현 (뉴스 처리 및 저장 트리거)
    -   `NewsService`에 `findAll` 메서드 구현 및 `NewsController`에 `GET /news` 엔드포인트 구현 (모든 뉴스 조회)
    -   `ArtistsService`에 `findAll` 메서드 구현 및 `ArtistsController`에 `GET /artists` 엔드포인트 구현 (모든 아티스트 조회)
    -   임시 테스트 엔드포인트 및 메서드 제거 (`AppController`의 `GET /test-db`, `AiController`의 `GET /ai/models` 및 `POST /ai/process-news`, `AiService`의 `listAvailableModels`)

### 5단계: 자동화된 뉴스 처리 시스템 구축 (완료)
-   **내용**: 뉴스 소스를 주기적으로 모니터링하고, 새로운 뉴스를 자동으로 크롤링, AI 처리, 번역 및 데이터베이스에 저장하는 시스템을 구축합니다.
-   **세부 작업**:
    -   `NewsSource` 엔티티 정의 (`url`, `artist` (nullable), `lastCrawledAt` 포함)
    -   `NewsSourceModule`, `NewsSourceService`, `NewsSourceController` 설정 (뉴스 소스 CRUD API)
    -   `NewsProcessorModule` 및 `NewsProcessorService` 구현 (자동화된 뉴스 처리 로직)
    -   `@nestjs/schedule` 및 `node-cron`을 사용하여 `NewsProcessorService`를 매분 실행하도록 스케줄링 설정
    -   `NewsProcessorService`에서 뉴스 목록 페이지를 크롤링하고, 개별 기사 링크를 추출하여 처리하도록 로직 구현
    -   AI가 기사 내용에서 아티스트를 식별하고 한국어 이름을 생성하도록 `AiService` 프롬프트 개선
    -   AI가 아티스트를 식별하지 못한 경우 뉴스를 저장하지 않도록 로직 추가
    -   `ArtistsService`에 `koreanName` 필드 추가 및 `findOrCreate` 메서드 업데이트 (아티스트 이름 및 한국어 이름 저장)
    -   데이터 초기화를 위한 `DELETE /news-source` 및 `DELETE /news` 엔드포인트 추가

---

## 핵심 기술 및 동작 원리 (간단 설명)

우리가 만든 시스템은 **J-pop 뉴스 기사를 웹에서 자동으로 찾아내고, 그 내용을 이해해서 한국어로 번역한 다음, 데이터베이스에 저장하는 자동화된 파이프라인**입니다.

**1. 전체 목표:**
*   J-pop 가수들의 새로운 소식(신곡, 콘서트 등)을 웹에서 자동으로 수집합니다.
*   수집된 일본어 뉴스를 한국어로 번역합니다.
*   번역된 뉴스를 데이터베이스에 저장하고, 웹사이트에서 보여줄 수 있도록 준비합니다.

**2. 핵심 구성 요소:**

*   **뉴스 소스 (News Source)**:
    *   **역할**: "어떤 웹사이트에서 뉴스를 가져올까?"를 알려주는 설정 정보입니다. 예를 들어, `natalie.mu/music/news`와 같은 뉴스 목록 페이지 URL을 여기에 등록합니다.
    *   **기술**: `NewsSource` 엔티티(데이터베이스 테이블)에 저장됩니다.

*   **크롤러 (Crawler)**:
    *   **역할**: 뉴스 소스에 등록된 웹사이트에 직접 접속해서, 그 페이지의 HTML(웹페이지의 원본 코드)을 가져오는 도구입니다. 마치 웹 브라우저가 웹페이지를 여는 것과 비슷하지만, 화면에 보여주는 대신 코드를 가져옵니다.
    *   **기술**: `axios` (웹 요청 보내기)와 `cheerio` (가져온 HTML 코드 분석하기) 라이브러리를 사용합니다.

*   **AI (Gemini)**:
    *   **역할**: 크롤러가 가져온 HTML 코드를 읽고, 그 안에서 진짜 뉴스 내용이 무엇인지, 어떤 아티스트에 대한 뉴스인지, 언제 발행되었는지 등을 **똑똑하게 이해하고 추출**합니다. 그리고 일본어 원문을 **자연스러운 한국어로 번역**해주는 핵심 비서 역할을 합니다.
    *   **기술**: Google의 `Gemini API` (저, Gemini 모델)를 사용합니다.

*   **데이터베이스 (DB)**:
    *   **역할**: AI가 처리하고 번역한 뉴스 정보(제목, 내용, 아티스트 이름 등)와 아티스트 정보를 안전하게 저장하는 창고입니다.
    *   **기술**: `PostgreSQL` (데이터 저장)과 `TypeORM` (Node.js에서 DB를 쉽게 다루게 해주는 도구)을 사용합니다.

*   **스케줄러 (Scheduler)**:
    *   **역할**: 이 모든 복잡한 과정을 우리가 직접 매번 실행할 필요 없이, "매분", "매시간" 등 정해진 시간마다 **자동으로 실행시켜주는 타이머**입니다.
    *   **기술**: NestJS의 `@nestjs/schedule` 모듈과 `node-cron` 라이브러리를 사용합니다.

**3. 동작 원리 (자동화된 뉴스 수집 파이프라인):**

1.  **자동 시작**: 스케줄러가 정해진 시간(예: 매분)이 되면, "뉴스 처리 시작!"을 알리며 `NewsProcessorService`를 실행합니다.
2.  **뉴스 소스 목록 가져오기**: `NewsProcessorService`는 데이터베이스에 등록된 모든 뉴스 소스(URL) 목록을 가져옵니다.
3.  **뉴스 목록 페이지 크롤링**: 각 뉴스 소스 URL(예: `https://natalie.mu/music/news`)에 접속하여, 그 페이지에 있는 개별 뉴스 기사들의 링크(URL)들을 찾아냅니다.
4.  **개별 기사 크롤링**: 찾아낸 각 뉴스 기사 링크에 하나씩 접속해서, 해당 기사의 전체 HTML 내용을 가져옵니다.
5.  **AI에게 분석 및 번역 요청**: 가져온 개별 기사의 HTML 내용을 AI(Gemini)에게 보냅니다.
    *   AI에게 "이 HTML에서 뉴스 제목, 내용, 게시일, 출처, 카테고리, 그리고 **이 기사의 주요 아티스트 이름(일본어)과 그 아티스트의 한국어 이름(음차 또는 통용명)**을 찾아서 JSON 형태로 알려줘"라고 지시합니다.
    *   AI는 이 지시에 따라 정보를 추출하고 번역하여 JSON 형태로 반환합니다.
6.  **아티스트 식별 및 뉴스 저장**:
    *   AI가 반환한 JSON 데이터에서 `artistName`과 `koreanArtistName`을 확인합니다.
    *   만약 AI가 아티스트를 식별하지 못했다면 (예: "Not Found" 반환), 해당 뉴스는 저장하지 않고 건너뜁니다.
    *   아티스트가 식별되었다면, `ArtistsService`를 통해 해당 아티스트가 데이터베이스에 있는지 확인합니다. 없으면 새로 등록하고, 있으면 기존 정보를 사용합니다. (이때 아티스트의 한국어 이름도 함께 저장됩니다.)
    *   `NewsService`를 통해 AI가 분석한 뉴스 정보(원문 제목/내용, 번역 제목/내용, 게시일, 출처 등)와 위에서 확인한 아티스트 정보를 연결해서 데이터베이스에 저장합니다.
7.  **반복 및 완료**: 모든 뉴스 소스와 그 안의 개별 기사들에 대해 이 과정을 반복하고, "뉴스 처리 완료!"를 알립니다.

이러한 과정을 통해, 우리는 웹에서 J-pop 뉴스를 자동으로 수집하고, AI의 도움을 받아 내용을 이해하고 번역하며, 체계적으로 데이터베이스에 저장하는 시스템을 구축했습니다。

---

이 가이드라인을 준수하여 코드베이스를 깨끗하고 일관성 있게 유지하며, 쉽게 탐색할 수 있도록 하겠습니다。

---

## 배포 계획 (Deployment Plan)

아래 계획에 따라 프로덕션 및 스테이징 환경 배포를 진행합니다.

### 1단계: 보안 취약점 점검 및 관리 (완료)
-   **내용**: 소스 코드에 민감 정보(API 키, 비밀번호 등)가 포함되지 않도록 점검하고 관리합니다.
-   **세부 작업**:
    -   소스 코드 내 민감 정보 하드코딩 여부 확인 (완료)
    -   `.gitignore` 파일에 `.env` 등 민감 파일 포함 여부 확인 (완료)
    -   `backend/.env.example` 파일을 생성하여 필요한 환경 변수 목록화 (완료)

### 2단계: GitHub 저장소 생성 및 코드 푸시 (사용자 수행)
-   **내용**: 모든 코드를 비공개 GitHub 저장소에 푸시합니다.
-   **세부 작업**:
    -   GitHub.com에서 신규 비공개(Private) 저장소 생성
    -   로컬 프로젝트를 원격 저장소와 연결하고 `main` 브랜치에 코드 푸시

### 3단계: 클라우드 데이터베이스(PostgreSQL) 배포 (사용자 수행)
-   **내용**: **Supabase**를 사용하여 관리형 PostgreSQL 데이터베이스를 생성합니다.
-   **세부 작업**:
    -   Supabase 가입 및 새 프로젝트 생성
    -   프로젝트의 **Settings > Database** 메뉴에서 데이터베이스 연결 문자열(Connection String) 확보

### 4단계: 백엔드 배포 (사용자 수행)
-   **내용**: **Render.com**을 사용하여 NestJS 백엔드 애플리케이션을 배포합니다.
-   **세부 작업**:
    -   Render 가입 및 'New Web Service' 생성
    -   GitHub 저장소 연결 및 아래와 같이 설정:
        -   **Branch**: `main` (프로덕션), `staging` (스테이징)
        -   **Root Directory**: `backend`
        -   **Build Command**: `npm install && npm run build`
        -   **Start Command**: `node dist/main`
    -   **Environment Variables**에 Supabase DB 연결 정보 및 API 키 설정

### 5단계: 프론트엔드 배포 (사용자 수행)
-   **내용**: **Vercel**을 사용하여 Next.js 프론트엔드 애플리케이션을 배포합니다.
-   **세부 작업**:
    -   Vercel 가입 및 'New Project' 생성
    -   GitHub 저장소 연결 (Vercel이 자동으로 Next.js 설정 감지)
    -   **Settings > Domains** 메뉴에서 프로덕션용 커스텀 도메인 설정
    -   `staging` 브랜치는 자동으로 생성되는 미리보기 URL을 스테이징 환경으로 사용

### 6단계: CORS 정책 설정 (완료)
-   **내용**: 백엔드에서 프로덕션 및 스테이징 프론트엔드 도메인 요청을 허용하도록 설정합니다.
-   **세부 작업**:
    -   `backend/src/main.ts`의 `enableCors` 로직을 수정하여, 환경 변수 `FRONTEND_URL`에 콤마로 구분된 여러 URL을 주입받을 수 있도록 변경 (완료)

### 7단계: GitHub Actions 자동 배포 파이프라인 구축 (완료)
-   **내용**: `staging` 브랜치의 변경 사항을 주기적으로 `production` 브랜치에 자동 병합합니다.
-   **세부 작업**:
    -   `.github/workflows/deploy.yml` 파일 생성 (완료)
    -   매주 화요일 오전 10시에 `staging` 브랜치를 `production` 브랜치로 병합하는 워크플로우 작성 (완료)
    -   GitHub 저장소 Secrets에 `GH_PAT` (Personal Access Token) 설정 필요 (사용자 수행)

### 8단계: `GEMINI.md`에 배포 계획 문서화 (완료)
-   **내용**: 위 모든 배포 계획을 `GEMINI.md` 파일에 기록하여 관리합니다. (완료)